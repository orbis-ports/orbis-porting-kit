# Patch series for swanstation
#
# Read by scripts/orbis-patch.sh, cmake/OrbisPatch.cmake and the patches CI job. Lines are
# `key: value`; everything after a # is a comment. Patch order is the file name order, which is
# what git format-patch produces and what every consumer here relies on.
#
# upstream/ref name the tree these patches were written against. They are taken from the recipe
# this organisation's CI actually uses, .libretro-super/recipes/linux/cores-linux-x64-generic,
# NOT from the first recipe that mentions the core - the apple recipes pin mupen64plus_next to
# branch `ios` and flycast to libretro's mirror, and a series that recorded those would be
# describing a tree nobody builds.
#
# upstream-status is per patch and is the field that stops a merged patch living here forever:
#   local        ours, not sent anywhere
#   sent <url>   a pull request exists
#   merged <sha> upstream took it; the patch is deletable and a script should say so
# orbis-ports/dynarmic carried six lines for two months after upstream fixed the same defect
# because there was nowhere to write this down.

source-of-truth: libretro-super recipes/linux/cores-linux-x64-generic at bc2b5463
upstream: https://github.com/libretro/swanstation.git
ref: main

patch: 0001-orbis-give-the-memory-arena-a-real-implementation.patch
  exit: none
  exit-why: src/common/memory_arena.cpp - shared, not executable, memory; no shm_open here. Not what orbis_jit answers
  upstream-status: local
patch: 0002-orbis-the-recompiler-targets-the-sysv-abi.patch
  exit: upstream (candidate)
  exit-why: cpu_recompiler_types.h - the target is SysV, which upstream keys on platform rather than on ABI
  upstream-status: local
patch: 0003-orbis-give-the-jit-code-buffer-a-platform.patch
  exit: overlay:orbis_jit
  exit-why: jit_code_buffer.cpp - replaces a hand-written mmap+mprotect
  upstream-status: local
patch: 0004-orbis-promote-s-fast-map-before-writing.patch
  exit: overlay:orbis_jit
  exit-why: cpu_code_cache.cpp promotes s_fast_map, including its hand-written 16 KiB rounding
  upstream-status: local
patch: 0005-orbis-a-4-kib-guard-on-a-16-kib-page-console.patch
  exit: none
  exit-why: page-size arithmetic; the literal can name ORBIS_JIT_GRANULE, the arithmetic stays
  upstream-status: local
patch: 0006-orbis-actually-disable-fastmem-on-failure.patch
  exit: none
  exit-why: cpu_code_cache.cpp - fastmem off on failure
  upstream-status: local
patch: 0007-orbis-fastmem-off-where-its-handler-cannot-exist.patch
  exit: none
  exit-why: fastmem off where its handler cannot exist
  upstream-status: local
